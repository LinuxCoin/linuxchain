language:
  - cpp

os:
  - linux
  - osx

compiler:
  - gcc
  - clang

script:
  - sudo apt-get install libboost1.55-all-dev -y
  - mkdir build
  - cd build
  - cmake ..
  - make && make test
  - src/bittubed --help
  - src/bittubed
